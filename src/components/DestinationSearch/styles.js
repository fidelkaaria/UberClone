import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
    height: '100%',
    width: '100%',
    backgroundColor: '#eee',
  },
  textInput: {
    padding: 10,
    backgroundColor: '#eee',
    marginVertical: 10,
  },
});

export default styles;
